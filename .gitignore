node_modules/
.env
*.log

# Jackie's actual live content — intentionally untracked so a `git pull`
# on the Pi can never overwrite what she's already written/uploaded.
data/content.json
public/uploads/*.jpg
public/uploads/*.jpeg
public/uploads/*.png
public/uploads/*.gif
public/uploads/*.webp
*.zip
